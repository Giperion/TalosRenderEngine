#ifndef __WINDOW_IFACE_HPP__
#define __WINDOW_IFACE_HPP__

///////////////////////////////////////////////////////////////////////////////////////////////////
// Заголовочные файлы																			 //
///////////////////////////////////////////////////////////////////////////////////////////////////

#include <Windows.h>

///////////////////////////////////////////////////////////////////////////////////////////////////
// Директивы препроцессора																		 //
///////////////////////////////////////////////////////////////////////////////////////////////////

// Стандартный тип передачи параметров в функцию
#define CALLTYPE __stdcall

///////////////////////////////////////////////////////////////////////////////////////////////////
// Объявления																					 //
///////////////////////////////////////////////////////////////////////////////////////////////////

//
// Интерфейс окна
// Позволяет управлять окном
//
struct IWindow
{
	// Инициализирует окно
	virtual bool Initialize(void) = 0;

	// Уничтожает экземпляр окна
	virtual void Release(void) = 0;

	// Обрабатывает сообщения
	virtual bool Update(void) = 0;

	// Возвращает хендл окна
	virtual HWND GetHandle(void) = 0;
};

//
// Создает новый экземпляр интерфейса к окну и возвращает указатель на него
//
IWindow *CALLTYPE WindowCreate(void);

#endif /* __WINDOW_IFACE_HPP__ */